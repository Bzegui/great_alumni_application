import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'great_alumni_app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
